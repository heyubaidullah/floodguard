import { QueueManager, QueueStats } from '../queue-system/queue-manager';
export declare class EventConsumer {
    private readonly taskId;
    private readonly queueManager;
    private readonly handler;
    private queue;
    private isRunning;
    private stats;
    private processingTimes;
    constructor(taskId: string, queueManager: QueueManager & {
        updateStats(taskId: string, updates: Partial<QueueStats>): Promise<void>;
    }, handler: (event: unknown) => Promise<void>);
    start(): Promise<void>;
    stop(): Promise<void>;
    private updateStats;
    private calculateThroughput;
    private calculateAvgProcessingTime;
}
//# sourceMappingURL=event-consumer.d.ts.map