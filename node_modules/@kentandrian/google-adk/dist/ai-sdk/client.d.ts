import { type ChatRequestOptions, HttpChatTransport, type UIMessage, type UIMessageChunk } from "ai";
import type { AdkClient } from "..";
/**
 * A transport class for handling chat messages in the ADK client.
 * This class extends HttpChatTransport to provide methods for sending messages
 * and processing response streams.
 */
export declare class AdkChatTransport extends HttpChatTransport<UIMessage> {
    private adk;
    constructor(adk: AdkClient);
    processResponseStream(stream: ReadableStream<Uint8Array>): ReadableStream<UIMessageChunk>;
    sendMessages({ messages, chatId, sessionId, }: {
        messages: UIMessage[];
        chatId: string;
        sessionId?: string;
    } & ChatRequestOptions): Promise<ReadableStream<UIMessageChunk>>;
}
