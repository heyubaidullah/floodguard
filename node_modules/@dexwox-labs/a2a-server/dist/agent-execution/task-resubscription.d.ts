import { RequestContext } from './request-context';
import { EventQueue } from './event-queue';
import { TaskManager } from '../tasks/task-manager';
export declare class TaskResubscriptionHandler {
    private taskManager;
    private readonly requestHandler;
    constructor(taskManager: TaskManager);
    /**
     * Handle task resubscription request
     * @param context Request context
     * @param queue Event queue for streaming updates
     */
    handleResubscription(context: RequestContext, queue: EventQueue): AsyncGenerator<any, void, unknown>;
}
//# sourceMappingURL=task-resubscription.d.ts.map